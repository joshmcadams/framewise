/** Real Framewise defaults to 30s. Shared between delay-render.ts and render.mjs. */
export declare const DEFAULT_DELAY_RENDER_TIMEOUT: number;
/** Extra margin added to the renderer's waitForFunction deadline (see render.mjs). */
export declare const RENDERER_TIMEOUT_MARGIN_MS: number;
