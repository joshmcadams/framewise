# Implementation Plans

Generated by the improve skill (deep audit) on 2026-07-09, against commit
`985ca38`. Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update your row when done.

Baseline at planning time: 51/51 tests passing, `npm audit` clean, working
tree clean. Verification commands: `npm test`, `npm run build`; plan 001 adds
`npm run typecheck` and `npm run verify` (single-command gate) — most later
plans cite them.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Verification baseline: typecheck/verify scripts, CI, engines, tsconfig fix | P1 | S | — | DONE (merged `f1021dd`, on `review-fixes`) |
| 002  | Extract render.mjs pure logic into render-lib.mjs + unit tests | P1 | M | 001 | DONE (merged `dd32944`, on `review-fixes`) |
| 003  | Renderer robustness: signal cleanup, fault-isolated teardown, codec preflight, strict flags | P1 | M | 002 | DONE (merged `4e5541c`, on `review-fixes`) |
| 004  | Characterization tests: Img, Audio, Video, CompositionHost | P1 | M | 001 | DONE (executed `983ac37` in worktree; test files need merge) |
| 005  | Fix the Video render-seek race | P1 | M | 004 | DONE (executed `adc82bd` in worktree; Video.tsx + tests merged) |
| 006  | Docs drift: teach CompositionHost, regenerate the source map | P1 | M | — | DONE (executed, merged; all 4 docs chapters synced) |
| 007  | ESLint (flat) + Prettier, wired into verify/CI | P2 | M | 001 | DONE (executed, merged; lint + format gates active) |
| 008  | Author CLAUDE.md | P2 | S | — | DONE (executed, merged; agent guidance with invariants, commands, conventions) |
| 009  | Gate Chrome's --no-sandbox | P2 | S | 003 | DONE (executed, merged; sandbox on by default, explicit opt-in) |
| 010  | staticFile()/random(): demo usage, walkthrough coverage, barrel hygiene | P2 | M | 006 | DONE (executed, merged; all 4 compositions wired, docs updated) |
| 011  | Extract shared preview media-sync into useMediaSync() | P2 | S | 004, 005 | DONE (executed, merged; Audio + Video now share one hook) |
| 012  | Player: interactive-surface tests + opt-in maxHeight fitting | P2 | S | 001 | DONE (executed, merged; 8 new tests, maxHeight prop) |
| 013  | Test spring()'s fractional-frame branch | P2 | S | 001 | DONE (executed, merged; 7 new tests, toBeCloseTo(1) for known ODE divergence) |
| 014  | React 18.3 → 19 upgrade | P3 | M | 004 | DONE (executed, merged; clean upgrade, zero type fixes needed) |
| 015  | Output formats: --format webm/gif/png-seq + --still | P2 | M | 002, 003 | TODO |
| 016  | Easing library (backlog item C) | P2 | M | 001 (order after 006, 010) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **001 first, always.** It creates `npm run verify`, which nearly every other
  plan uses as its gate.
- **004 before 005**: plan 004 lands a characterization test that *documents*
  the Video seek race; plan 005 fixes the bug and flips that test. Executing
  005 without 004's tests loses the regression net.
- **002 before 003**: extraction (behavior-preserving) lands the tests that
  003's behavior changes (strict flags, scoped --list parser) then update.
- **003 before 009 and 015**: all three edit `scripts/render.mjs`; 003
  restructures cleanup and flag handling that 009/015 build on.
- **005 before 011**: both edit `Video.tsx`; 011 extracts the *preview* effect
  and must not race 005's restructuring of the *render* effect.
- **006 before 010 before 016** (ordering, not hard deps): all three edit
  `docs/code/README.md`'s source map; sequential execution avoids conflicts.
- **004 before 014**: the React major bump wants the media components covered
  before the framework moves under them.
- 007, 008, 012, 013 are independent of everything except 001 and can run in
  parallel with the main chain.

## Findings considered and rejected

Recorded so nobody re-audits them:

- **delayRender timeout should remove the handle from the pending set**
  (delay-render.ts:44-54 only logs): rejected — the log-and-stay-pending
  behavior is load-bearing. The renderer's backstop failing loudly (with
  labels, via render.mjs's catch) is the correct outcome for an abandoned
  handle; auto-removing would let renders silently capture broken frames. The
  stuck preview badge is arguably correct UI (the handle IS stuck).
- **Spring integer-chain cache is unbounded** (spring.ts:123): rejected for
  now — acknowledged in the code comment, mirrors upstream's design, and
  bounded in practice (pathological per-frame-animated configs cost tens of
  KB per render). Revisit only with evidence from a real workload; plan 013's
  tests pin the behavior any future eviction change must preserve.
- **Memoize the Player controls subtree** (re-created per frame): rejected —
  negligible reconciliation cost; the extraction would hurt the
  teaching-clarity goal of the file.
- **`assetPath` path traversal** (render.mjs:64 strips only a leading `/`):
  not planned — near by-design for a local CLI run by the file's owner
  (reads only files the invoking user can already read). Worth a one-line
  containment check if plan 002/015 touches that code anyway, not a plan.
- **Renderer perf trio** (Vite dev server instead of a built bundle; two CDP
  round-trips per frame foldable into one; a whole browser launched to probe
  static registry metadata): valid findings, not selected for planning this
  round. The probe also has a by-design tension (future dynamic metadata à la
  calculateMetadata). Available for a future `plan` invocation.
- **Audio-registry stale report on within-frame unmount**
  (audio-registry.ts:30-44): extremely narrow trigger; documented as a
  characterization test in plan 004 (Step 2, case 5) instead of a fix.
- **Direction items not selected**: `<Series>`/`<Loop>`, preview props
  editor, `interpolateColors` (backlog B), `measureSpring` family (backlog E),
  and the three README "deliberately omitted" fidelity spikes (OffthreadVideo
  extraction, volume automation, distributed rendering) — surfaced in the
  audit with evidence; the maintainer selected output formats (015) and
  Easing (016) for this round.
